# Frame Pi: display.py, panel-independent deps only.
# On a Pi Zero W (ARMv6) numpy needs libatlas3-base, and the panel needs SPI
# (+ I2C for Inky) enabled. install.sh handles all of that plus the
# panel-specific driver package (inky, or spidev+gpiozero for the vendored
# Waveshare epd7in3e driver) - prefer running install.sh on a fresh Pi rather
# than installing this file by hand.
Pillow>=10,<12
tomli; python_version < "3.11"
